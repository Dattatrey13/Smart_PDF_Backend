# Core
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
gunicorn>=21.2.0
python-multipart>=0.0.6
python-dotenv>=1.0.0

# Data validation
pydantic[email]>=2.0.0

# PDF Processing
PyPDF2>=3.0.1

# AI / ML
google-genai>=1.0.0
numpy>=2.0.0

# Firebase
firebase-admin>=6.2.0

# Email (HTTP-based, works on Render/cloud platforms that block SMTP)
resend>=2.0.0

# Security & Performance (no extra deps needed — uses stdlib + starlette built-ins)
